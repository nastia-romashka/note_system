services:
  mongo:
    image: mongo:7
    container_name: note_service_mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: notes_system
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
